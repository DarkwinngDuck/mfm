"""Application constants."""

# Serial encoding
ENCODE = "ascii"

# Default positions
HOME_POSITION = (0, 0, 0)
DEFAULT_MESH_START = (1.0, 1.0, 1.0)

# Default speeds and timeouts
DEFAULT_MOVE_SPEED = 150
DEFAULT_BAUDRATE = 9600
DEFAULT_TIMEOUT = 10
